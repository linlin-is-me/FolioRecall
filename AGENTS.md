# FolioRecall 开发协作约定

本文件放在仓库根目录，记录稳定的开发与协作规则。项目范围、模型和候选方法以最新开发计划为准，当前实现以代码和实测记录为准；具体任务按用户本轮要求推进。

项目仓库：[linlin-is-me/FolioRecall](https://github.com/linlin-is-me/FolioRecall)。HTTPS 克隆地址：`https://github.com/linlin-is-me/FolioRecall.git`。

本机工作目录为 `D:\myproject\FolioRecall`，WSL 默认挂载路径为 `/mnt/d/myproject/FolioRecall`，两者使用同一份代码。已有工作副本直接接续；需要克隆时使用该目标目录，避免重复嵌套目录。

本地在 WSL 2 Ubuntu 22.04 中使用独立环境 `foliorecall_env`，首次会话创建或复用。环境目录建议 `~/.venvs/foliorecall_env`，创建前确认磁盘位置与余量，再将实际路径、激活方式和已验证版本记入 README；不把建议路径或创建计划当作已验证状态。环境仅在首次接入、配置变化或异常时验证，已验证且未变化时直接复用；GPU 检查未通过不阻塞可在 CPU 上完成的任务。保留已有环境，AutoDL 路径按实例记录。若无法访问本机 WSL，提供用户可执行的命令，不将云端安装表述为本机完成。

## 项目方向

FolioRecall（页寻）是视觉文档检索开源项目，包含文档导入、页面编码与索引、模型微调、查询蒸馏、评测及可用演示，用真实开发成果体现工程能力。

当前先完成英文页面检索，提高检索质量并降低在线查询成本。中文、重排序、答案生成及其他方法按实际需求扩展，候选清单不作为全部必须完成的任务。

## 开始与接续

- 远程仓库已创建，已有对应本地工作副本时优先接续，尚无副本时克隆上述仓库。新会话先查看 README、开发计划第 1 节的当前开发重点、Git 状态和近期相关提交，再阅读本轮涉及的代码与说明。README 或历史提交尚不存在时，按开发计划第 8 节的起步阶段完成本轮需要的项目初始化，不因缺少历史记录中断。无需每次重读全部方法储备。
- 开发计划通过 README 提供入口；尚未入库时使用用户提供的最新版，首次整理仓库时补上链接。
- 确认当前环境、已有结果和本轮目标，保留工作区中原有的未提交改动。计划中的功能不视为已经实现，历史结果不视为在当前环境重新验证过。
- 在已明确范围内直接推进常规实现与可逆修改；只有关键歧义影响任务范围或执行结果时再澄清。

## 实现与验证

- 优先复用现成库，用普通函数、少量脚本和配置跑通可用流程。有实际重复或维护困难后再重构，不提前建设通用插件框架、复杂 CI 或完整部署平台。
- 以单卡和小规模试跑起步，记录耗时后再决定扩大训练或启用扩展。暂时缺少 GPU 时先完成可进行的工作，如实说明尚未验证的模型步骤。
- 检查由首次接入、相关改动或异常触发。修复验证通过后结束额外检查，不为每次提交补跑全量测试、逐文件哈希或全量缓存扫描。
- 保证训练、开发与测试数据分离，查询与页面 ID 配对正确，教师、学生和索引采用匹配配置。首次接入或修改训练核心逻辑时，用小样本检查必要的计算、梯度与保存加载；未改动且已验证的路径复用结果。
- 缓存用模型标识、关键配置和目录区分；输入、权重或预处理变化时更新受影响部分。遇到错配或损坏迹象再定向排查。
- 方法实验保留合理基线，控制相关条件；工程优化测量实际瓶颈。配置与指标用现有实验记录保存，说明未完成验证、无效尝试和质量代价，不预设正向收益。
- 关键实验记录运行所用的 Git 提交、实际生效配置与产物位置，保留运行时配置副本或记录覆盖参数。存在影响结果的未提交代码时，注明并保留相关差异，不将提交号单独视为完整复现依据；普通试跑无需逐次提交或新增记录体系。

## 真实提交

- 完成一个有意义的改动后及时创建本地提交。同一功能相关的代码、配置与必要说明可以一起提交，无关改动分别处理；不攒到项目结束一次提交，也不刻意拆分以增加数量。
- 提交前查看本次差异，仅暂存本轮相关内容，保留用户已有工作。模型权重、原始数据和缓存按项目约定另行保存，代码提交保留其获取或配置入口。
- 每个标题说明具体对象与变化，例如 `feat: 支持 PDF 页面导入`、`fix: 修正负例页面映射`。前缀可选，避免仅写 update 或优化代码。
- 关键提交用几句话说明问题、改法与已有验证结果，可引用配置路径或实验编号。普通小改动无需长正文；中间试验注明状态，回退或放弃方案时记录原因。
- 复用上游代码时保留来源，明确自己的适配范围。未测量的收益不写成实测结果，不要求每次提交都有性能提升，也不设置提交次数、频率或额外审批。
- 远程推送与公开发布按当前会话已有授权执行，本地提交不自动代表获准发布。

## README 与会话收尾

仓库初始化时建立 README 初稿，先写项目用途、当前能力与限制、已有环境和可用命令、上游来源及开发计划入口。未完成的功能或命令标明状态，后续有真实成果再补演示、架构图、个人贡献和核心结果表。

功能、启动方式或主要结果变化时更新对应内容；普通内部修改无需重写 README。详细方法和复现实验说明放在现有文档中，本文件不重复维护模型参数和实验清单。

收尾时先将需要接续的当前任务、阻塞问题与下一步更新在开发计划第 1 节，将本轮需要保存的文档改动纳入相关提交。README 展示可用功能并链接到开发计划，不另设任务清单。相关命令、配置和产物位置沿用已有文档与实验记录，通过链接引用；状态没有变化时无需修改文档，不另建交接体系。

结束简报说明已完成的改动、验证结果或未验证原因、下一步，以及当前分支、最新本地提交、需接续的未提交内容和远程同步状态。保留用户原有工作，不要求清空工作区；未核实远程状态时如实注明。本轮最新提交号可在结束回复中提供，无需为写回该提交号反复追加提交。

交流与文档使用简洁中文，技术标识保留原名。区分已实现、已验证和待探索内容，结论对应实际证据。
